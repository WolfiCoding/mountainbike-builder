{
    "version": "2.0.0",
    "tasks": [
      {
        "label": "Build and Run Mountainbike App (PowerShell-safe)",
        "type": "shell",
        "command": "powershell",
        "args": [
          "-Command",
          "if (Test-Path mountainbikeApp.exe) { Remove-Item mountainbikeApp.exe -Force }; g++ -g *.cpp -o mountainbikeApp.exe; if ($?) { ./mountainbikeApp.exe }"
        ],
        "group": {
          "kind": "build",
          "isDefault": true
        },
        "problemMatcher": ["$gcc"],
        "detail": "PowerShell-safe: builds all .cpp files and runs the app"
      }
    ]
  }
  